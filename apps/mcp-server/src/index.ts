// MCP server stub - simplified for build compatibility
import { rewrite, type RewriteInput } from "@prompt-orchestrator/core";

console.error("orchestrator-mcp: stub mode (MCP SDK integration pending)");

// For now, just exit successfully to allow builds
process.exit(0);
